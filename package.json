{
  "name": "quizflare-netlify",
  "version": "1.0.0",
  "description": "QuizFlare with Netlify Functions",
  "private": true,
  "scripts": {
    "start": "parcel index.html",
    "build": "parcel build index.html --dist-dir publish"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^6.23.1"
  },
  "scripts": {
    "start": "parcel index.html",
    "build": "parcel build index.html --dist-dir publish"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}